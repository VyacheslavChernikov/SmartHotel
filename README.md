# SmartHotel 360

SmartHotel — это минимальная система бронирования отелей с веб-админкой, REST API и Telegram-ботом для поиска свободных номеров и оформления бронирований.

Проект состоит из двух основных частей:

- **Django + DRF backend** (`admin_backend/`)  
- **Telegram-бот на aiogram 3.7** (`admin_backend/bot/`)

---

## 🚀 Функционал

### Backend (Django + DRF)
- Модели: Hotel, Room, Booking
- API:
  - `GET /api/hotels/` — список отелей
  - `GET /api/rooms/?hotel=<id>` — список свободных номеров
  - `POST /api/booking/` — создание брони
- Django admin для полной ручной работы с системой

### Telegram-бот
- Получает список отелей
- Показывает только свободные номера
- Пошагово собирает данные гостя
- Делает запросы в наш API и создаёт бронирование
- Поддержка режима **AI-ассистента** (интеграция ГигаЧат)

---

## 📦 Установка и запуск backend

```bash
git clone https://github.com/VyacheslavChernikov/SmartHotel.git
cd SmartHotel/admin_backend

python -m venv venv
source venv/bin/activate
pip install -r ../requirements.txt

python manage.py migrate
python manage.py createsuperuser

python manage.py runserver
После запуска API будет доступно по адресу:
http://127.0.0.1:8000/api/

⚙️ Переменные окружения

Создайте файл .env внутри admin_backend/:
BOT_TOKEN=ТВОЙ_TELEGRAM_ТОКЕН
API_BASE_URL=http://127.0.0.1:8000/api

# ГигаЧат
GIGACHAT_AUTH_KEY=...
GIGACHAT_AUTH=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
GIGACHAT_API=https://gigachat.devices.sberbank.ru/api/v1/chat/completions

🤖 Запуск Telegram-бота
cd admin_backend/bot
source ../venv/bin/activate
python bot.py

📂 Структура проекта
SmartHotel/
│
├── admin_backend/
│   ├── admin_backend/       # Django конфиг
│   ├── hotels/              # App: отели
│   ├── rooms/               # App: номера
│   ├── bookings/            # App: брони
│   ├── bot/                 # Telegram-бот
│   └── manage.py
│
├── api_backend/             # (черновик — пока не используется)
├── requirements.txt
└── README.md

🔐 Безопасность

.env не коммитится

токены и ключи не публикуются

HTTPS-запросы к ГигаЧату выполняются без проверки сертификата (можно включить при необходимости)

🛠 Планы на развитие

Авторизация менеджеров

Панель подтверждений бронирований

История бронирований в Telegram

Поддержка оплат
